"""
Floats are immutable data type, 
that said it cannot be edited / manipulated, in any way
after creation.
"""


# Below is a demo of creating a integer type data type.

random_float: float = 0 # Default value [whose bool(var_name) is False always]
random_float_2: float = 10.12 # Non-Defualt value [whose bool(var_name) is True always]