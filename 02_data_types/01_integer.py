"""
Integers are immutable data type, 
that said it cannot be edited / manipulated, in any way
after creation.
"""


# Below is a demo of creating a integer type data type.

random_int: int = 0 # Default value [whose bool(var_name) is False always]
random_int_2: int = 10 # Non-Defualt value [whose bool(var_name) is True always]